#1. El primer error que tiene el codigo es que los print para imprimir de manera correcta tiene que tener parentesis 
#2. En ningun momento hay una definicion del num o hay una entrada de este numero ais que sera imposible correr el codgio si no ponemos una varibale de entrada o un numero random que nos defina este valor entero.
#3. En los parametros del codigo nos piden que dependiendo a que se cumpla nos imprima Falso o verdadero y no hay nigun tipo de salida de nigun valor verdadero o falso 


def negate (num)
 return num
def large_num(num)
 res=(num>10000)
negate (b)
neg_b= num
print 'b:' , b , 'neg_b' , neg_b
big = large_num(b)
print 'b if big:' , big


#correcion

def negate(num):
    return -num

def large_num(num):
    return num > 10000

#Corregir el nombre de la variable de entrada de la función negate
b=(input("Digite el valor que quiere cambiar"))
neg_b = negate(b)
print('b:', b, 'neg_b:', neg_b)

big = large_num(b)
print('b if big:', big)


#Elaborado por Maria Jose Bastilla-1098668885
